# server.py e server_chat.py não têm dependência nenhuma (só biblioteca
# padrão) — é o que roda hoje, sem microfone na Jetson.

# Transcrição (pc/stt.py, pc/server_voz.py) — só quando a Jetson tiver mic
faster-whisper>=1.0

# Ferramenta local de captura+transcrição (pc/push_to_talk.py)
sounddevice>=0.4.6
numpy>=1.24
